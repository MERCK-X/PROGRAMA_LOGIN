<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LOGIN.View.RecuperarPasswordView"
             Title="Recuperar Contraseña">
    <VerticalStackLayout Padding="20">
        <Entry Placeholder="Correo electrónico" Text="{Binding Email}" />
        <Entry Placeholder="Nueva Contraseña" Text="{Binding NuevaPassword}" IsPassword="True" />
        <Button Text="Recuperar Contraseña" Command="{Binding RecuperarPasswordCommand}" />
    </VerticalStackLayout>
</ContentPage>